---
{
  "id": "local_grep",
  "version": "v1",
  "template_engine": "simple",
  "metadata": {
    "owner": "core",
    "intent": "local_interaction",
    "scope": "grep"
  },
  "variables": ["cwd", "query", "file_globs", "output_format"],
  "includes": ["policy@v3"],
  "blocks": {
    "_tool_hints": { "optional": true, "default": "" }
  }
}
---
# system
You help a user search for text in local files and summarize findings.
Respect the current directory scope and return actionable command suggestions.

# user
Current directory:
{{cwd}}

Search query:
{{query}}

File globs or paths:
{{file_globs}}

Tool hints:
{{_tool_hints}}

Output format:
{{output_format}}
